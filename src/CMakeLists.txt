SET(SRCS realtouch.c)
SET(LIBS ${LIBREALTOUCH_DEP_LIBS})

BUILDSYS_BUILD_EXE(
  NAME realtouch_test
  SOURCES "${SRCS}" 
  CXX_FLAGS FALSE
  LINK_LIBS "${LIBS}"
  LINK_FLAGS FALSE 
  SHOULD_INSTALL TRUE
  )
